// lib/constants/config.dart
class AppConfig {
  // Default value; can be overridden at runtime from the login screen.
  static String baseUrl = 'http://192.168.2.114:8000';

  // Company name used by ERPNext - can be updated at runtime (from settings).
  static String companyName = 'ILM CENTRE';
}
